# Model weights - downloaded at build time, not stored in git
models/*
!models/.gitkeep

# Dataset files - downloaded at build time, not stored in git
datasets/*
!datasets/.gitkeep

# Generated at build time
Containerfile

# Python bytecode (reshard_safetensors.py is importable)
__pycache__/
*.pyc

# Archived weights from previous builds
models_archive/
datasets_archive/

# Saved image splits for air-gapped transfer
save/